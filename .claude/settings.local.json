{
  "permissions": {
    "allow": [
      "PowerShell(Add-Type -AssemblyName WindowsBase; $zip = [System.IO.Compression.ZipFile]::OpenRead\\(\"C:\\\\Users\\\\MagidMukhtarKhidarHa\\\\Downloads\\\\VMS_Technical_Specification.docx\"\\); $entry = $zip.GetEntry\\(\"word/document.xml\"\\); $reader = New-Object System.IO.StreamReader\\($entry.Open\\(\\)\\); $xml = $reader.ReadToEnd\\(\\); $reader.Close\\(\\); $zip.Dispose\\(\\); $xml = $xml -replace '<[^>]+>','' -replace '&lt;','<' -replace '&gt;','>' -replace '&amp;','&' -replace '\\\\s+',' '; $xml.Trim\\(\\) | Select-Object -First 1)",
      "Bash(docker-compose -f docker-compose.local.yml down)",
      "Bash(docker-compose -f docker-compose.local.yml up -d)",
      "Bash(docker-compose -f docker-compose.local.yml ps)",
      "Bash(curl -s http://localhost:4000/api/health)"
    ]
  }
}
