{
  "name": "vms-monorepo",
  "version": "1.0.0",
  "description": "Visitor Management System - Monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "install": "npm --prefix frontend install",
    "dev": "echo 'Use docker-compose for local development'"
  }
}
